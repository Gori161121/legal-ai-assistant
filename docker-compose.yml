services:
  api:
    build: .
    container_name: legal-intake-api
    environment:
      # OPENAI_API_KEY: set to enable AI-generated intake summaries (optional)
      PYTHONPATH: /app
    ports:
      - "8000:8000"
    restart: unless-stopped
